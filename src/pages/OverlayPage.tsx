import { PresetOverlay } from '@/components/PresetOverlay';

const OverlayPage = () => {
    return (
        <div className="w-full h-full">
            <PresetOverlay />
        </div>
    );
};

export default OverlayPage; 